export const imageMimeTypeRegex = /^((?:x-)?image)\/([a-z0-9-]+)$/i;
export const fileExt = /\.([^.\\/:*?"<>|\r\n]+)$/; // not the first or the second part (create new line) and this ($) being end of string.
export const sampleIdRegex = /^:sample:(.+)/;
